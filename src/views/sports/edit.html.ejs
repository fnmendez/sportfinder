<h1>Editing sport: <%= sport.name %></h1>

<% if (locals.errors) { %>
    <div class="errors">
      Necesitas arreglar los siguiente errores:
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.message %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  <form action="<%= updateSportPath %>" method="post">
    <input type="hidden" name="_method" value="patch" />
    <div>
      <label for="title">Nombre</label>
      <input type="text" name="name" value="<%= sport.name %>" />
    </div>
    <div>
      <input type="submit" name="create" value="Guardar" />
    </div>
  </form>
</div>

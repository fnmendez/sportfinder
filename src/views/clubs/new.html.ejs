<h1>Creando un club</h1>

<h2>Nuevo Club:</h2>
<div>
  <% if (locals.errors) { %>
    <div class="errors">
      Necesitas arreglar los siguiente errores:
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.message %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  <form action="<%= createPurposalPath %>" method="post">
    <div>
      <label for="name">Nombre</label>
      <input type="text" name="name" value="<%= club.name %>" />
    </div>
    <div>
      <label for="address">Dirección</label>
      <textarea type="text" name="description"><%= club.address %></textarea>
    </div>
      <input type="submit" name="create" value="Crear club" />
    </div>
  </form>
</div>

<h1>Editing club: <%= club.name %></h1>

<% if (locals.errors) { %>
    <div class="errors">
      Necesitas arreglar los siguiente errores:
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.message %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  <form action="<%= updateClubPath %>" method="post">
    <input type="hidden" name="_method" value="patch" />
    <div>
      <label for="name">Nombre</label>
      <input type="text" name="name" value="<%= club.name %>" />
    </div>
    <div>
      <label for="address">Dirección</label>
      <input type="text" name="address" value="<%= club.address %>" />
    </div>
    <div>
      <input type="submit" name="create" value="Guardar" />
    </div>
  </form>
</div>

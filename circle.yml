machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 9.0.0
  yarn:
    version: 1.2.1

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - yarn lint

deployment:
  production:
    branch: master
    heroku:
      appname: sportfinder-app
